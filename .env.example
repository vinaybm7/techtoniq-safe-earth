# ===================================
# Environment Configuration
# ===================================

# AI Configuration (Required)
VITE_GEMINI_API_KEY=your_google_gemini_api_key_here

# API Configuration (Optional - handled by build system)
# VITE_API_BASE_URL=/api

# Supabase Configuration (Already configured in API)
# SUPABASE_URL=https://wqsuuxgpbgsipnbzzjms.supabase.co
# SUPABASE_ANON_KEY=your_supabase_anon_key_here

# MongoDB Atlas Connection String (Legacy - now using Supabase)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/techtoniq?retryWrites=true&w=majority

# Server Configuration
NODE_ENV=development
PORT=3001

# Security
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# ===================================
# Optional Settings
# ===================================

# Logging
ENABLE_LOGGING=true
LOG_LEVEL=debug

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX=100           # limit each IP to 100 requests per windowMs

# ===================================
# Notes:
# 1. Never commit actual credentials to version control
# 2. Create a .env.local file for local development
# 3. Set environment variables in your deployment platform
# ===================================
