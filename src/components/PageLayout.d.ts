import { ReactNode } from "react";
interface PageLayoutProps {
    children: ReactNode;
}
declare const PageLayout: ({ children }: PageLayoutProps) => import("react/jsx-runtime").JSX.Element;
export default PageLayout;
